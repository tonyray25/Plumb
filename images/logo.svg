<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 280 80">
  <!-- P -->
  <text x="0" y="60" font-family="'DM Sans', Arial, sans-serif" font-size="64" font-weight="700" fill="#ffffff">P</text>

  <!-- Plumb bob (replacing l) -->
  <g transform="translate(42, 0)">
    <!-- String -->
    <rect x="8" y="0" width="4" height="45" fill="#f0a500"/>
    <!-- Bob top -->
    <rect x="2" y="43" width="16" height="8" fill="#f0a500"/>
    <!-- Bob body (pentagon/diamond shape) -->
    <polygon points="10,51 20,56 20,68 10,80 0,68 0,56" fill="#f0a500"/>
  </g>

  <!-- umb -->
  <text x="68" y="60" font-family="'DM Sans', Arial, sans-serif" font-size="64" font-weight="700" fill="#ffffff">umb</text>
</svg>
